{% extends 'base.html.twig' %} {% block title %}Au Panier Gourmand{% endblock %} {% block body %}

<div class="container mt-5">
    <div class="categories">
        {% for category in category %}
        <figure class="snip1091 red"><img src="https://i.ibb.co/NKTPx1p/ardoise1.png" />
            <figcaption>
                <h2>{{category.name}}</h2>
                <a href="{{ path('category_show', { 'categoryName': category.name }) }}"></a>
            </figcaption>
        </figure>
        {% endfor %}
    </div>
</div>{% endblock %}