{% extends 'base.html.twig' %} {% block title %}Livraison{% endblock %} {% block body %}
<h1>Livraison</h1>

<table class="table">
    <thead>
        <tr>
            <th>Département</th>
            <th>Commune</th>
            <th>Code postal</th>
            <th>Date de livraison</th>
        </tr>
    </thead>
    <tbody>
        {% for shipping in shippings %}
        <tr>
            <td>{{ shipping.departement }}</td>
            <td>{{ shipping.commune }}</td>
            <td>{{ shipping.zipCode }}</td>
            <td>
                <input type="date" id="start" name="trip-start" value="today" min="2018-01-01" max="2050-12-31">
            </td>
        </tr>
        {% else %}
        <tr>
            <td colspan="3">no records found</td>
        </tr>
        {% endfor %}
    </tbody>
</table>{% endblock %}